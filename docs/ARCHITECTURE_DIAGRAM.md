# Jotty Architecture - Visual Diagrams

## 🚨 Current Architecture (WRONG)

```
┌─────────────────────────────────────────────────────────┐
│                    APPLICATIONS                         │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐      │
│  │ Frontend │      │ Telegram │      │   CLI    │◄─┐   │
│  │  (apps/) │      │  (apps/) │      │ (core/)  │  │   │
│  └────┬─────┘      └────┬─────┘      └────┬─────┘  │   │
│       │                 │                  │        │   │
│       ↓                 ↓                  ↓        │   │
└───────┼─────────────────┼──────────────────┼────────┼───┘
        │                 │                  │        │
        │                 │                  │        │
┌───────┼─────────────────┼──────────────────┼────────┼───┐
│       ↓                 ↓                  ↓        │   │
│  ┌─────────────────────────────────────────────┐   │   │
│  │              SDK Layer                      │   │   │
│  │         (jotty.Jotty client)                │   │   │
│  └─────────────────────────────────────────────┘   │   │
│       ↓                 ↓                          │   │
└───────┼─────────────────┼──────────────────────────┼───┘
        │                 │                          │
        │                 │                          ↓
┌───────┼─────────────────┼──────────────────────────────┐
│       ↓                 ↓                  ┌──────────┐ │
│  ┌─────────────────────────────┐          │   CLI    │ │
│  │      Core Framework         │          │ DIRECT   │ │
│  │  • intelligence/            │          │ IMPORTS! │ │
│  │  • modes/                   │◄─────────┤   ❌     │ │
│  │  • capabilities/            │          │ Bypasses │ │
│  │  • infrastructure/          │          │   SDK    │ │
│  └─────────────────────────────┘          └──────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘

❌ PROBLEM: CLI is in core/ and directly imports from core modules
❌ PROBLEM: CLI bypasses SDK layer completely
❌ PROBLEM: SDK not being dogfooded by internal apps
```

---

## ✅ Recommended Architecture (CORRECT)

```
┌─────────────────────────────────────────────────────────────┐
│                  LAYER 5: APPLICATIONS                      │
│                                                             │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐             │
│  │   CLI    │    │ Frontend │    │ Telegram │             │
│  │ (apps/)  │    │ (apps/)  │    │ (apps/)  │             │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘             │
│       │               │               │                    │
│       │ Uses SDK      │ Uses SDK      │ Uses SDK           │
│       ↓               ↓               ↓                    │
└───────┼───────────────┼───────────────┼─────────────────────┘
        │               │               │
        └───────────────┼───────────────┘
                        ↓
┌─────────────────────────────────────────────────────────────┐
│               LAYER 4: SDK (Stable Public API)              │
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │  from jotty import Jotty                          │     │
│  │                                                   │     │
│  │  client = Jotty()                                 │     │
│  │  result = await client.chat("...")                │     │
│  │  result = await client.workflow("...")            │     │
│  │  async for event in client.stream("..."):         │     │
│  │      print(event)                                 │     │
│  └───────────────────────────────────────────────────┘     │
│                        ↓                                    │
└────────────────────────┼─────────────────────────────────────┘
                         │ Only imports from
                         │ core/interface/api/
                         ↓
┌─────────────────────────────────────────────────────────────┐
│         LAYER 3: CORE API (Internal for SDK)                │
│                                                             │
│  ┌───────────────────────────────────────────────────┐     │
│  │  core/interface/api/                              │     │
│  │  • unified.py        → JottyAPI                   │     │
│  │  • chat_api.py       → ChatAPI                    │     │
│  │  • workflow_api.py   → WorkflowAPI                │     │
│  └───────────────────────────────────────────────────┘     │
│                        ↓                                    │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│              LAYER 2: CORE FRAMEWORK                        │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ intelligence/│  │    modes/    │  │capabilities/ │     │
│  │ • memory     │  │ • agent      │  │ • skills     │     │
│  │ • learning   │  │ • workflow   │  │ • registry   │     │
│  │ • swarms     │  │ • execution  │  │ • tools      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                             │
│  ┌──────────────────────────────────────────────────┐      │
│  │         infrastructure/                          │      │
│  │         • foundation, utils, context             │      │
│  └──────────────────────────────────────────────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘

✅ CORRECT: Clean layer separation
✅ CORRECT: Apps use SDK only
✅ CORRECT: SDK is dogfooded by internal apps
✅ CORRECT: Core can change without breaking apps
```

---

## 🔄 Import Flow

### ❌ Current (Wrong)

```python
# apps/cli/app.py (WRONG - CLI in core/)
from Jotty.core.intelligence.orchestration import Orchestrator  # ❌ Direct core import
from Jotty.core.infrastructure.foundation.data_structures import SwarmConfig
from Jotty.core.capabilities.registry.skills_registry import get_skills_registry

# Bypasses SDK completely!
swarm = Orchestrator(agents="...")
result = await swarm.run(goal="...")
```

### ✅ Recommended (Correct)

```python
# apps/cli/app.py (CORRECT - CLI in apps/)
from jotty import Jotty  # ✅ Uses SDK

client = Jotty()
result = await client.workflow(goal="...", agents="...")
```

---

## 📦 Directory Structure Comparison

### ❌ Current Structure

```
Jotty/
├── core/
│   ├── intelligence/
│   ├── modes/
│   ├── capabilities/
│   ├── infrastructure/
│   └── interface/
│       ├── api/              ← Core API
│       ├── cli/              ← ❌ CLI in core! (WRONG)
│       └── use_cases/
├── sdk/                      ← SDK layer
│   └── client.py
└── apps/
    ├── frontend/
    └── telegram_bot/
```

### ✅ Recommended Structure

```
Jotty/
├── core/
│   ├── intelligence/
│   ├── modes/
│   ├── capabilities/
│   ├── infrastructure/
│   └── interface/
│       ├── api/              ← Core API (for SDK)
│       └── use_cases/
├── sdk/                      ← SDK layer
│   └── client.py
└── apps/                     ← All applications
    ├── cli/                  ← ✅ CLI moved here!
    │   ├── main.py
    │   ├── commands/
    │   ├── repl/
    │   └── ui/
    ├── frontend/
    └── telegram_bot/
```

---

## 🎯 Dependency Rules

```
┌─────────────────────────────────────┐
│  Rule 1: Apps ONLY import from SDK  │
└─────────────────────────────────────┘
  ✅ from jotty import Jotty
  ❌ from Jotty.core.* import *

┌─────────────────────────────────────┐
│  Rule 2: SDK ONLY imports core API  │
└─────────────────────────────────────┘
  ✅ from Jotty.core.interface.api import JottyAPI
  ❌ from Jotty.core.intelligence import *

┌─────────────────────────────────────┐
│  Rule 3: No circular dependencies   │
└─────────────────────────────────────┘
  apps/ ──> sdk/ ──> core/
    ↑                   │
    └───────────────────┘ ❌ FORBIDDEN
```

---

## 🔄 Migration Steps

```
STEP 1: Move CLI
┌────────────┐         ┌────────────┐
│ core/      │         │ apps/      │
│ interface/ │  ──────>│ cli/       │
│ cli/       │  MOVE   │            │
└────────────┘         └────────────┘

STEP 2: Update Imports
┌────────────────────────────┐         ┌────────────────────┐
│ from Jotty.core.*          │ ──────> │ from jotty import  │
│ Direct core imports (❌)    │ REPLACE │ SDK imports (✅)    │
└────────────────────────────┘         └────────────────────┘

STEP 3: Test
┌────────────┐         ┌────────────┐
│ Run Tests  │ ──────> │ Verify OK  │
│ CLI still  │  CHECK  │ No breaks  │
│ works      │         │            │
└────────────┘         └────────────┘
```

---

## 📊 Benefits Visualization

```
┌──────────────────────────────────────────────────────────┐
│                  CURRENT (WRONG)                         │
│                                                          │
│  Core changes → CLI breaks (direct coupling)             │
│  SDK unused → No dogfooding → SDK quality unknown       │
│  Mixed layers → Unclear boundaries                       │
└──────────────────────────────────────────────────────────┘
                         ↓
                  MIGRATION
                         ↓
┌──────────────────────────────────────────────────────────┐
│                RECOMMENDED (CORRECT)                     │
│                                                          │
│  Core changes → SDK stable → Apps don't break ✅         │
│  SDK dogfooded → Quality assured ✅                      │
│  Clear layers → Easy to maintain ✅                      │
└──────────────────────────────────────────────────────────┘
```

---

## 🎓 Example: Adding New Feature

### ❌ Current Approach (Wrong)

```
1. Add feature to core/
2. CLI imports directly from core/
3. SDK not updated (not used)
4. External devs can't use new feature
5. SDK diverges from reality
```

### ✅ Recommended Approach (Correct)

```
1. Add feature to core/
2. Expose via core/interface/api/
3. Add to SDK client
4. CLI uses new SDK method
5. External devs get feature automatically
6. SDK stays in sync
```

---

## 🔍 Quick Reference

| Question | Answer |
|----------|--------|
| **Where should CLI be?** | `apps/cli/` (not `core/interface/cli/`) |
| **What should CLI import?** | Only from `jotty` (SDK) |
| **What should SDK import?** | Only from `Jotty.core.interface.api/` |
| **Can apps import from core?** | ❌ NO - must use SDK |
| **Can SDK import from apps?** | ❌ NO - one-way dependency |
| **What's the benefit?** | Stable API, dogfooding, clear boundaries |

---

## 🚀 Ready to Migrate?

**Checklist:**
- [ ] Understand clean architecture principles
- [ ] Review current vs recommended structure
- [ ] Create `apps/cli/` directory
- [ ] Move CLI code from `core/interface/cli/`
- [ ] Update CLI imports to use SDK
- [ ] Test CLI still works
- [ ] Update documentation
- [ ] Add architecture tests (import linting)

**Estimated Time:** 2-4 hours
**Risk Level:** Low (mainly moving files + updating imports)
**Impact:** High (proper architecture foundation)

---

**Visual diagrams last updated:** 2026-02-15
