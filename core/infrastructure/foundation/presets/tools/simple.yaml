# Tools: Simple
# ==============
# Hardcoded tool list, no auto-discovery

implementation: simple
registry: manual

tools:
  auto_register: false
  allow_custom: true
  max_tools: 50

discovery:
  enabled: false

interception:
  enabled: false
