# Allow access from proxy domain
DANGEROUSLY_DISABLE_HOST_CHECK=true

# Alternative: specify allowed hosts
# ALLOWED_HOSTS=strategies.planmyinvesting.com,localhost

# WebSocket configuration
WDS_SOCKET_PORT=0

# Public URL for proxy access
# Set to /proxy/3000 when accessing via proxy
# Set to / or leave empty for direct localhost access
PUBLIC_URL=/proxy/3000
